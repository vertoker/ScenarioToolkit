.row {
    flex-direction: row;
    justify-content: center;
    align-items: center;
    flex-grow: 0;
}

.grow {
    flex-grow: 1;
}

StructElement {
    margin: 2px;
    background-color: rgba(0, 0, 0, 0.3);
}
